Place your generated intro voice here as intro.mp3

Generate it once:  pip install -r requirements.txt  then  python scripts/generate_intro.py
(needs GROQ_API_KEY in .env). The site works without it; the voice just stays silent.
